/*eslint no-unused-vars: 1*/
var RaceSimulator = (function () {

    'use strict';

    var theRaceSimulator = function(selection) {
        console.log('received simulator selection');
    };

    return theRaceSimulator;

}());
